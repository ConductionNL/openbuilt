<?xml version="1.0"?>
<ruleset name="OpenBuilt Nextcloud Rules"
    xmlns="http://pmd.sourceforge.net/ruleset/2.0.0"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://pmd.sourceforge.net/ruleset/2.0.0
                        http://pmd.sourceforge.net/ruleset_xml_schema.xsd">

    <description>
        This is a custom ruleset for OpenBuilt Nextcloud.
    </description>

    <!-- The embedded nextcloud-app-template snapshot is shipped as a verbatim
         resource; it is not OpenBuilt code, so we exclude it from MD scans. -->
    <exclude-pattern>*/lib/Resources/template/*</exclude-pattern>

    <!-- Clean Code Rules -->
    <rule ref="rulesets/cleancode.xml/BooleanArgumentFlag"/>
    <rule ref="rulesets/cleancode.xml/ElseExpression"/>
    <rule ref="rulesets/cleancode.xml/StaticAccess"/>
    <rule ref="rulesets/cleancode.xml/IfStatementAssignment"/>
    <rule ref="rulesets/cleancode.xml/DuplicatedArrayKey"/>
    <rule ref="rulesets/cleancode.xml/MissingImport"/>
    <rule ref="rulesets/cleancode.xml/UndefinedVariable"/>
    <rule ref="rulesets/cleancode.xml/ErrorControlOperator"/>

    <!-- Code Size Rules -->
    <rule ref="rulesets/codesize.xml/CyclomaticComplexity"/>
    <rule ref="rulesets/codesize.xml/NPathComplexity"/>
    <rule ref="rulesets/codesize.xml/ExcessiveMethodLength"/>
    <rule ref="rulesets/codesize.xml/ExcessiveParameterList"/>
    <rule ref="rulesets/codesize.xml/ExcessivePublicCount"/>
    <rule ref="rulesets/codesize.xml/TooManyFields"/>
    <rule ref="rulesets/codesize.xml/TooManyMethods"/>
    <rule ref="rulesets/codesize.xml/TooManyPublicMethods"/>
    <!--
        ExcessiveClassLength threshold raised to 1500: ApplicationsController
        is the single app-local HTTP surface (design.md Decision 6) and now
        also hosts the clone-from-template endpoint (createFromTemplate, spec
        #8 openbuilt-templates-marketplace) with its ~13 OR-integration helper
        methods (per-app register provisioning, companion-schema cloning,
        manifest schema-ref rewriting). Splitting across controllers would
        just scatter the same OR plumbing; the default 1000 fits a vanilla CRUD
        controller, not a four-endpoint integration surface crossing two apps.
    -->
    <rule ref="rulesets/codesize.xml/ExcessiveClassLength">
        <properties>
            <property name="minimum" value="1500" />
        </properties>
    </rule>
    <!--
        ExcessiveClassComplexity threshold raised to 120: ApplicationsController
        is the single app-local HTTP surface (design.md Decision 6) and now
        hosts the manifest endpoint, the RBAC-filtered list endpoint
        (openbuilt-rbac), the manifest-diff endpoint (openbuilt-versioning) and
        the clone-from-template endpoint (createFromTemplate, spec #8
        openbuilt-templates-marketplace), each with its own slug→entity
        resolution, error envelopes and OR-integration helpers. Splitting
        across multiple controllers would just scatter the same OR-integration
        plumbing; the default 50 fits a single-purpose CRUD controller.
    -->
    <rule ref="rulesets/codesize.xml/ExcessiveClassComplexity">
        <properties>
            <property name="maximum" value="120" />
        </properties>
    </rule>

    <!-- Controversial Rules -->
    <rule ref="rulesets/controversial.xml/Superglobals"/>
    <rule ref="rulesets/controversial.xml/CamelCaseClassName"/>
    <rule ref="rulesets/controversial.xml/CamelCasePropertyName"/>
    <rule ref="rulesets/controversial.xml/CamelCaseMethodName"/>
    <!-- CamelCaseParameterName removed - we allow leading underscores on parameters (convention for unused/config params) -->
    <!-- <rule ref="rulesets/controversial.xml/CamelCaseParameterName"/> -->
    <!-- CamelCaseVariableName removed - we allow leading underscores on variables (convention for unused loop vars) -->
    <!-- <rule ref="rulesets/controversial.xml/CamelCaseVariableName"/> -->

    <!-- Design Rules -->
    <rule ref="rulesets/design.xml/ExitExpression"/>
    <rule ref="rulesets/design.xml/EvalExpression"/>
    <rule ref="rulesets/design.xml/GotoStatement"/>
    <rule ref="rulesets/design.xml/NumberOfChildren"/>
    <rule ref="rulesets/design.xml/DepthOfInheritance"/>
    <!--
        CouplingBetweenObjects threshold raised to 20 to accommodate the
        ApplicationsController, which integrates OR (RegisterMapper,
        SchemaMapper, ObjectService, AuditTrailMapper, ObjectEntity) +
        NC OCP types + DateTime + Psr\Log + Throwable. The PHPMD default
        of 13 fits a vanilla CRUD controller; an integration controller
        crossing two NC apps and OCP cannot stay under that without
        artificial indirection.
    -->
    <rule ref="rulesets/design.xml/CouplingBetweenObjects">
        <properties>
            <property name="maximum" value="20" />
        </properties>
    </rule>
    <rule ref="rulesets/design.xml/DevelopmentCodeFragment"/>
    <rule ref="rulesets/design.xml/EmptyCatchBlock"/>
    <rule ref="rulesets/design.xml/CountInLoopExpression"/>

    <!-- Naming Rules -->
    <rule ref="rulesets/naming.xml/LongClassName"/>
    <rule ref="rulesets/naming.xml/ShortClassName"/>
    <!-- ShortVariable configured with allowlist of idiomatic short names -->
    <rule ref="rulesets/naming.xml/ShortVariable">
        <properties>
            <property name="minimum" value="3" />
            <property name="exceptions" value="id,db,qb,op,ui,io,gc,tz,pk,fk,to,ch,a,b,l,v,c,t,r,f,n,k,e" />
        </properties>
    </rule>
    <rule ref="rulesets/naming.xml/LongVariable"/>
    <rule ref="rulesets/naming.xml/ShortMethodName"/>
    <rule ref="rulesets/naming.xml/ConstructorWithNameAsEnclosingClass"/>
    <rule ref="rulesets/naming.xml/ConstantNamingConventions"/>
    <rule ref="rulesets/naming.xml/BooleanGetMethodName"/>

    <!-- Unused Code Rules -->
    <rule ref="rulesets/unusedcode.xml/UnusedPrivateField"/>
    <rule ref="rulesets/unusedcode.xml/UnusedLocalVariable"/>
    <rule ref="rulesets/unusedcode.xml/UnusedPrivateMethod"/>
    <rule ref="rulesets/unusedcode.xml/UnusedFormalParameter">
        <exclude-pattern>*Migration*</exclude-pattern>
    </rule>
</ruleset>
