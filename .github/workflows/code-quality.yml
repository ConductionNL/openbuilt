name: Code Quality

on:
  push:
    branches: [main, development, feature/**, bugfix/**, hotfix/**]
  pull_request:
    branches: [main, master, development, beta]
  workflow_dispatch:

jobs:
  quality:
    uses: ConductionNL/.github/.github/workflows/quality.yml@main
    with:
      app-name: openbuilt
      php-version: "8.3"
      php-test-versions: '["8.3", "8.4"]'
      nextcloud-test-refs: '["stable31", "stable32", "stable33"]'
      enable-psalm: true
      enable-phpstan: true
      enable-phpmetrics: true
      enable-frontend: true
      enable-eslint: true
      enable-phpunit: true
      enable-newman: true
      database: sqlite
      # The Newman collections drive the `hello-world` seed app + the
      # OpenBuilt registers/schemas; re-running the repair steps (via a
      # disable/enable cycle, which fires the post-migration steps) after the
      # server + OpenRegister are up guarantees SeedHelloWorld /
      # PopulateApplicationPermissions / SeedApplicationTemplates /
      # InitializeSettings have run — otherwise the API calls 404 on missing
      # registers and the prerequest scripts JSONError on the 404 bodies.
      newman-seed-command: 'php occ app:disable openbuilt && php occ app:enable openbuilt'
      # OpenRegister `development` carries the runtime-schema-API (auto-create
      # Register on application-type imports — OR PR #1464) and the lifecycle
      # engine (ObjectTransitionedEvent) that OpenBuilt's repair steps + the
      # Newman publish step need; `main` predates both. See openbuilt#29.
      additional-apps: '[{"repo":"ConductionNL/openregister","app":"openregister","ref":"development"}]'
      enable-sbom: true
